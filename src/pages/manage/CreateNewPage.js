import React, { Component } from 'react';
import { CreateNewContainer } from "my-containers/manage"

class CreateNewPage extends Component {
    render() {
        return (
            <>
                <CreateNewContainer/>
            </>
        );
    }
}

export default CreateNewPage;